name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: lister
      run: ls
    - name: configure
      run: ./config -no-asm
    - name: make
      run: make
    - name: make test
      run: make test
      
